# Dependências do backend (main.py) + scraper + indexador, usadas na imagem
# Docker. Bem mais leve que um scraper com navegador: o site institucional da
# Harus é HTML estático/server-rendered (ver scraper_produtos_harus.py), então
# não precisa de Playwright/Chromium — só requests + regex.
fastapi==0.138.2
google-genai==2.10.0
pydantic==2.13.4
python-dotenv==1.2.2
qdrant-client==1.18.0
requests==2.34.2
tenacity==9.1.4
truststore==0.10.4
uvicorn==0.49.0
